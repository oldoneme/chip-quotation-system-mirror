# ==================================================
# 芯片报价系统 - 环境变量模板
# ==================================================
# 说明：这是一个模板文件，不包含真实密钥
# 使用方法：
#   1. 复制此文件为 .env.dev 或 .env.prod
#   2. 替换所有 __REPLACE_ME__ 占位符为真实值
#   3. 确保 .env.dev 和 .env.prod 已在 .gitignore 中被忽略
# ==================================================

# —— 通用配置 ——
APP_ENV=development              # development | production
DEBUG=True
DEVELOPMENT_MODE=True

# —— 企业微信应用凭证 ——
WECOM_CORP_ID=wwxxxxxxxxxxxxxxxxx
WECOM_AGENT_ID=1000000
WECOM_SECRET=__REPLACE_ME__
WECOM_CORP_SECRET=__REPLACE_ME__

# —— 企业微信回调配置 ——
WECOM_CALLBACK_TOKEN=__REPLACE_ME__
WECOM_ENCODING_AES_KEY=__REPLACE_ME_43_CHARS__
WECOM_APPROVAL_TEMPLATE_ID=__REPLACE_ME__

# —— 域名/回调配置（DEV/PROD 需要不同值）——
WECOM_CALLBACK_URL=https://wecom-dev.chipinfos.com.cn/wecom/callback
WECOM_BASE_URL=https://wecom-dev.chipinfos.com.cn
FRONTEND_BASE_URL=https://wecom-dev.chipinfos.com.cn
API_BASE_URL=https://wecom-dev.chipinfos.com.cn/api

# —— 数据库配置（建议 DEV/PROD 使用不同数据库）——
DATABASE_URL=sqlite:///./data/dev/app.db

# —— JWT 配置 ——
SECRET_KEY=__REPLACE_ME_RANDOM_SECRET__
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# —— DeepLink JWT 配置 ——
DEEPLINK_JWT_SECRET=__REPLACE_ME_RANDOM_SECRET__
DEEPLINK_JWT_TTL_SECONDS=900

# —— 前端快照配置 ——
SNAPSHOT_BROWSER_POOL=3
SNAPSHOT_READY_SELECTOR=#quote-ready

# —— 审批配置 ——
APPROVAL_LINK_EXPIRE_DAYS=7

# —— 其他配置 ——
USE_WECHAT_MOCK=False
